package com.mvet.presidency.projects.repository.ProjectPhaseRepository;

import com.mvet.presidency.projects.entity.ProjectPhase;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProjectPhaseRepository extends JpaRepository<ProjectPhase, Long> {
}
